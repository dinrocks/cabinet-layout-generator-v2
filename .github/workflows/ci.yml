name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  web:
    name: web (lint · typecheck · test · build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test
      - run: npm run build

  service:
    name: service (deps · import · harnesses)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: service
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: service/requirements.txt
      - run: pip install -r requirements.txt
      - name: import smoke test
        run: python -c "import app; print('service imports OK')"
      - name: assembler harness (self-contained sample DXF)
        run: python test_build.py
      - name: JWT auth harness
        run: python test_auth.py
      - name: rail-datum capture harness
        run: python test_upload_rail.py
      - name: block download harness (bundle export)
        run: python test_block.py
